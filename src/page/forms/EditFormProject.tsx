import { FormEdit } from "../../shared/components";
import { MainLayout } from "../../shared/layout";

export const EditFormProject = () => {
  return (
    <MainLayout>
      <FormEdit />
    </MainLayout>
  );
};
